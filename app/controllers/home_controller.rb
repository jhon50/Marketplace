class HomeController < ApplicationController
  def index
    #
    # url = 'https://www.fossil.com.br/api/catalog_system/pub/products/search/'
    # response = HTTParty.get(url)
    #
    #
    # l = Loja.first
    #
    # # importa os produtos para a loja
    # response.parsed_response.each do |item|
    #   l.produtos.create! item
    # end
    #
    #
    # l.save
    #

#--------------------------------------------------
      #l.produtos.create! content: "greatsss"

      #l.produtos.first

    # response.parsed_response.each do |item|
    #   produto = Produto.new(item)
    #   produto.save
    # end

    # if current_user
    #   redirect_to lojas_path
    # end
  end
end
